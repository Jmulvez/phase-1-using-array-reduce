const batteryBatches = [4, 5, 3, 4, 4, 6, 5];
let totalBatteries = batteryBatches;
function totalBatteries(bateries) {
    for (const batteries of batteryBatches) {
        totalBatteries += batteries.batteryBatches;
    };
return totalBatteries;
}
console.log(totalBatteries(batteries));
